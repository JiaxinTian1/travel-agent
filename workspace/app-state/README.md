# App State

`app/server.js` stores the current board state here by default:

```text
workspace/app-state/board.json
```

This directory is ignored by git because it contains local planning state and may later include personal trip data.
