#!/bin/sh
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
BASE="$SCRIPT_DIR/xhs-mcp"
mkdir -p "$BASE/data" "$BASE/cache" "$BASE/home"

export XDG_CACHE_HOME="$BASE/cache"
export HOME="$BASE/home"
export COOKIES_PATH="$BASE/data/cookies.json"

if [ "${XHS_SKIP_LOGIN:-0}" != "1" ]; then
  "$BASE/xiaohongshu-login-linux-amd64"
fi

exec "$BASE/xiaohongshu-mcp-linux-amd64" -port :18060
